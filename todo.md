# TODO

## ZIP読み込み失敗時のUI改善

- 現在は、提出物内の `.zip` ファイルを開けない場合に `ManabaReportArchiveError` を送出して処理を中断する。
- 将来的には、UIレベルで次を実現する。

### 1. StudentTable上での明示通知

- どの生徒のどの提出ZIPが読み込めなかったかを、StudentTableにエラー状態として表示する。
- 「未提出」と「提出ZIPの読み込み失敗」を区別して表示する。

### 2. 展開後フォルダでの採点者修正フロー

- 可能な範囲で提出物を展開した後、問題のZIPファイル位置を採点者に提示する。
- 採点者が展開先フォルダ内で手動修正（再圧縮・置換など）できる導線を用意する。
- 修正後に再走査（再展開または再初期化）できる操作を提供する。

### 3. 期待する運用イメージ

- 初期化時にZIP読み込み失敗があれば、対象生徒のみ要修正としてマークする。
- 採点者が展開先で修正し、再チェックで解消できるようにする。
